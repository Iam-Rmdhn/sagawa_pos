services:
  redis:
    image: redis:7-alpine
    container_name: sagawa-redis
    restart: unless-stopped
    ports:
      - "6379:6379"
    command: redis-server --appendonly yes
    volumes:
      - sagawa_redis_data:/data

volumes:
  sagawa_redis_data:

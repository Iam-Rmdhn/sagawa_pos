import 'package:flutter/material.dart';

// App Colors
class AppColors {
  // ==================== LIGHT THEME COLORS ====================

  // Primary Colors (Light Theme)
  static const Color primary = Color(0xFFB8001F);
  static const Color primaryLight = Color(0xFFE6334D);
  static const Color primaryDark = Color(0xFF8A0017);

  // Secondary Colors
  static const Color secondary = Color(0xFFFF6F00);
  static const Color secondaryLight = Color(0xFFFF9E40);
  static const Color secondaryDark = Color(0xFFC43E00);

  // Background Colors (Light Theme)
  static const Color background = Color(0xFFFCFAEE);
  static const Color surface = Color(0xFFFFFFFF);
  static const Color error = Color(0xFFB00020);

  // Text Colors (Light Theme)
  static const Color textPrimary = Color(0xFF212121);
  static const Color textSecondary = Color(0xFF757575);
  static const Color textHint = Color(0xFFBDBDBD);

  // Status Colors
  static const Color success = Color(0xFF4CAF50);
  static const Color warning = Color(0xFFFFC107);
  static const Color info = Color(0xFF2196F3);

  // Price Colors (Light Theme)
  static const Color price = Color(0xFF2E7D32); // Dark Green for price
  static const Color priceLight = Color(0xFF4CAF50); // Lighter green
  static const Color priceDark = Color(0xFF1B5E20); // Darker green

  // Divider & Border (Light Theme)
  static const Color divider = Color(0xFFE0E0E0);
  static const Color border = Color(0xFFBDBDBD);

  // Card & Container (Light Theme)
  static const Color cardBackground = Colors.white;
  static const Color containerLight = Color(0xFFFAFAFA);

  // ==================== DARK THEME COLORS ====================

  // Primary Colors (Dark Theme)
  static const Color darkPrimary = Color(0xFFB8001F);
  static const Color darkPrimaryLight = Color(0xFFE6334D);
  static const Color darkPrimaryDark = Color(0xFF8A0017);

  // Background Colors (Dark Theme)
  static const Color darkBackground = Color(0xFF121212);
  static const Color darkSurface = Color(0xFF1E1E1E);
  static const Color darkError = Color(0xFFCF6679);

  // Text Colors (Dark Theme)
  static const Color darkTextPrimary = Color(0xFFE1E1E1);
  static const Color darkTextSecondary = Color(0xFFB0B0B0);
  static const Color darkTextHint = Color(0xFF707070);

  // Divider & Border (Dark Theme)
  static const Color darkDivider = Color(0xFF2C2C2C);
  static const Color darkBorder = Color(0xFF3C3C3C);

  // Card & Container (Dark Theme)
  static const Color darkCardBackground = Color(0xFF2C2C2C);
  static const Color darkContainerLight = Color(0xFF252525);

  // Price Colors (Dark Theme)
  static const Color darkPrice = Color(
    0xFF66BB6A,
  ); // Lighter green for dark mode
  static const Color darkPriceLight = Color(0xFF81C784); // Even lighter
  static const Color darkPriceDark = Color(0xFF4CAF50); // Medium green
}
